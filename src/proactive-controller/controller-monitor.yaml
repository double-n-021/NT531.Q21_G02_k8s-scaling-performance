apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: proactive-controller-monitor
  namespace: monitoring
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: proactive-controller
  endpoints:
    - port: metrics
      interval: 10s
